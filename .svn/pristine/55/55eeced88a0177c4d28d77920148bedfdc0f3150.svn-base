/******/ (function(modules) { // webpackBootstrap
/******/ 	// The module cache
/******/ 	var installedModules = {};

/******/ 	// The require function
/******/ 	function __webpack_require__(moduleId) {

/******/ 		// Check if module is in cache
/******/ 		if(installedModules[moduleId])
/******/ 			return installedModules[moduleId].exports;

/******/ 		// Create a new module (and put it into the cache)
/******/ 		var module = installedModules[moduleId] = {
/******/ 			exports: {},
/******/ 			id: moduleId,
/******/ 			loaded: false
/******/ 		};

/******/ 		// Execute the module function
/******/ 		modules[moduleId].call(module.exports, module, module.exports, __webpack_require__);

/******/ 		// Flag the module as loaded
/******/ 		module.loaded = true;

/******/ 		// Return the exports of the module
/******/ 		return module.exports;
/******/ 	}


/******/ 	// expose the modules object (__webpack_modules__)
/******/ 	__webpack_require__.m = modules;

/******/ 	// expose the module cache
/******/ 	__webpack_require__.c = installedModules;

/******/ 	// __webpack_public_path__
/******/ 	__webpack_require__.p = "/";

/******/ 	// Load entry module and return exports
/******/ 	return __webpack_require__(0);
/******/ })
/************************************************************************/
/******/ ([
/* 0 */
/***/ function(module, exports, __webpack_require__) {

	'use strict';

	var _public = __webpack_require__(1);

	var _public2 = _interopRequireDefault(_public);

	var _logo2 = __webpack_require__(5);

	var _logo3 = _interopRequireDefault(_logo2);

	var _nav2 = __webpack_require__(8);

	var _nav3 = _interopRequireDefault(_nav2);

	var _slider2 = __webpack_require__(10);

	var _slider3 = _interopRequireDefault(_slider2);

	var _image_text_list2 = __webpack_require__(12);

	var _image_text_list3 = _interopRequireDefault(_image_text_list2);

	var _time_list2 = __webpack_require__(14);

	var _time_list3 = _interopRequireDefault(_time_list2);

	var _article2 = __webpack_require__(16);

	var _article3 = _interopRequireDefault(_article2);

	var _share2 = __webpack_require__(18);

	var _share3 = _interopRequireDefault(_share2);

	var _article_more2 = __webpack_require__(20);

	var _article_more3 = _interopRequireDefault(_article_more2);

	var _index_nav = __webpack_require__(22);

	var _index_nav2 = _interopRequireDefault(_index_nav);

	var _index_content2 = __webpack_require__(24);

	var _index_content3 = _interopRequireDefault(_index_content2);

	var _nav_class2 = __webpack_require__(26);

	var _nav_class3 = _interopRequireDefault(_nav_class2);

	var _video_class_content2 = __webpack_require__(28);

	var _video_class_content3 = _interopRequireDefault(_video_class_content2);

	var _video_detail_content2 = __webpack_require__(30);

	var _video_detail_content3 = _interopRequireDefault(_video_detail_content2);

	var _game_class_content2 = __webpack_require__(32);

	var _game_class_content3 = _interopRequireDefault(_game_class_content2);

	var _game_detail_content2 = __webpack_require__(34);

	var _game_detail_content3 = _interopRequireDefault(_game_detail_content2);

	var _game_class_shade2 = __webpack_require__(36);

	var _game_class_shade3 = _interopRequireDefault(_game_class_shade2);

	var _video_class_shade2 = __webpack_require__(38);

	var _video_class_shade3 = _interopRequireDefault(_video_class_shade2);

	var _init_rem = __webpack_require__(40);

	var _init_rem2 = _interopRequireDefault(_init_rem);

	var _search2 = __webpack_require__(41);

	var _search3 = _interopRequireDefault(_search2);

	var _slider4 = __webpack_require__(43);

	var _slider5 = _interopRequireDefault(_slider4);

	var _load_more2 = __webpack_require__(44);

	var _load_more3 = _interopRequireDefault(_load_more2);

	var _scroll_to2 = __webpack_require__(47);

	var _scroll_to3 = _interopRequireDefault(_scroll_to2);

	var _classify2 = __webpack_require__(48);

	var _classify3 = _interopRequireDefault(_classify2);

	function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

	// 缓慢移动到目标的函数


	var initRem = new _init_rem2.default(); // 初始化rem

	// 缓慢移动到目标的函数
	// 初始化rem
	initRem.init(); // 初始化rem
	(0, _search3.default)();
	(0, _load_more3.default)();
	(0, _slider5.default)();
	(0, _scroll_to3.default)('index-scroll'); // 给class是index-scroll的元素绑定点击事件，让其缓慢去到目标的位置
	(0, _classify3.default)();

/***/ },
/* 1 */
/***/ function(module, exports) {

	// removed by extract-text-webpack-plugin

/***/ },
/* 2 */,
/* 3 */,
/* 4 */,
/* 5 */
/***/ function(module, exports) {

	// removed by extract-text-webpack-plugin

/***/ },
/* 6 */,
/* 7 */,
/* 8 */
/***/ function(module, exports) {

	// removed by extract-text-webpack-plugin

/***/ },
/* 9 */,
/* 10 */
/***/ function(module, exports) {

	// removed by extract-text-webpack-plugin

/***/ },
/* 11 */,
/* 12 */
/***/ function(module, exports) {

	// removed by extract-text-webpack-plugin

/***/ },
/* 13 */,
/* 14 */
/***/ function(module, exports) {

	// removed by extract-text-webpack-plugin

/***/ },
/* 15 */,
/* 16 */
/***/ function(module, exports) {

	// removed by extract-text-webpack-plugin

/***/ },
/* 17 */,
/* 18 */
/***/ function(module, exports) {

	// removed by extract-text-webpack-plugin

/***/ },
/* 19 */,
/* 20 */
/***/ function(module, exports) {

	// removed by extract-text-webpack-plugin

/***/ },
/* 21 */,
/* 22 */
/***/ function(module, exports) {

	// removed by extract-text-webpack-plugin

/***/ },
/* 23 */,
/* 24 */
/***/ function(module, exports) {

	// removed by extract-text-webpack-plugin

/***/ },
/* 25 */,
/* 26 */
/***/ function(module, exports) {

	// removed by extract-text-webpack-plugin

/***/ },
/* 27 */,
/* 28 */
/***/ function(module, exports) {

	// removed by extract-text-webpack-plugin

/***/ },
/* 29 */,
/* 30 */
/***/ function(module, exports) {

	// removed by extract-text-webpack-plugin

/***/ },
/* 31 */,
/* 32 */
/***/ function(module, exports) {

	// removed by extract-text-webpack-plugin

/***/ },
/* 33 */,
/* 34 */
/***/ function(module, exports) {

	// removed by extract-text-webpack-plugin

/***/ },
/* 35 */,
/* 36 */
/***/ function(module, exports) {

	// removed by extract-text-webpack-plugin

/***/ },
/* 37 */,
/* 38 */
/***/ function(module, exports) {

	// removed by extract-text-webpack-plugin

/***/ },
/* 39 */,
/* 40 */
/***/ function(module, exports) {

	'use strict';

	Object.defineProperty(exports, "__esModule", {
	        value: true
	});

	var _createClass = function () { function defineProperties(target, props) { for (var i = 0; i < props.length; i++) { var descriptor = props[i]; descriptor.enumerable = descriptor.enumerable || false; descriptor.configurable = true; if ("value" in descriptor) descriptor.writable = true; Object.defineProperty(target, descriptor.key, descriptor); } } return function (Constructor, protoProps, staticProps) { if (protoProps) defineProperties(Constructor.prototype, protoProps); if (staticProps) defineProperties(Constructor, staticProps); return Constructor; }; }();

	function _classCallCheck(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new TypeError("Cannot call a class as a function"); } }

	var initRem = function () {
	        function initRem() {
	                _classCallCheck(this, initRem);
	        }

	        _createClass(initRem, [{
	                key: 'init',

	                /**
	                 * @func
	                 * @desc 初始化rem
	                 */
	                value: function init() {
	                        var dpr = void 0,
	                            rem = void 0,
	                            scale = void 0,
	                            docEl = document.documentElement,
	                            fontEl = document.createElement('style'),
	                            metaEl = document.querySelector('meta[name="viewport"]');

	                        dpr = window.devicePixelRatio || 1;
	                        rem = docEl.clientWidth * dpr / 10;
	                        scale = 1 / dpr;

	                        // 设置viewport，进行缩放，达到高清效果
	                        metaEl.setAttribute('content', 'width=' + dpr * docEl.clientWidth + ',initial-scale=' + scale + ',maximum-scale=' + scale + ', minimum-scale=' + scale + ',user-scalable=no');

	                        // 设置data-dpr属性，留作的css hack之用
	                        docEl.setAttribute('data-dpr', dpr);

	                        // 动态写入样式
	                        docEl.firstElementChild.appendChild(fontEl);
	                        fontEl.innerHTML = 'html{font-size:' + rem + 'px!important;}';

	                        // 给js调用的，某一dpr下rem和px之间的转换函数
	                        window.rem2px = function (v) {
	                                v = parseFloat(v);
	                                return v * rem;
	                        };
	                        window.px2rem = function (v) {
	                                v = parseFloat(v);
	                                return v / rem;
	                        };

	                        window.dpr = dpr;
	                        window.rem = rem;
	                }
	        }]);

	        return initRem;
	}();

		exports.default = initRem;

/***/ },
/* 41 */
/***/ function(module, exports, __webpack_require__) {

	'use strict';

	Object.defineProperty(exports, "__esModule", {
	    value: true
	});

	var _jQuery = __webpack_require__(42);

	var _jQuery2 = _interopRequireDefault(_jQuery);

	function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

	var _search = function _search() {
	    var search = (0, _jQuery2.default)('.search-no-focus span'),
	        // 搜索按钮
	    searchFocus = (0, _jQuery2.default)('.search-focus'),
	        searchInput = (0, _jQuery2.default)('.search-focus input'),
	        searchCancel = (0, _jQuery2.default)('.search-focus a'),
	        logo = (0, _jQuery2.default)('.logo');

	    if (!search || !searchFocus || !searchInput || !searchCancel || !logo) {
	        return;
	    }

	    // 绑定搜索按钮点击事件
	    search.bind('click', function () {
	        search.css('display', 'none');
	        searchFocus.css('visibility', 'visible');
	        logo.css('visibility', 'hidden');
	        searchInput.focus();
	    });

	    // 绑定搜索输入框的失去焦点事件
	    searchInput.bind('blur', function () {
	        search.css('display', 'block');
	        searchFocus.css('visibility', 'hidden');
	        logo.css('visibility', 'visible');
	    });

	    // 绑定搜索框的删除按钮
	    searchCancel.bind('click', function () {
	        search.css('display', 'block');
	        searchFocus.css('visibility', 'hidden');
	        logo.css('visibility', 'visible');
	    });
	};

	exports.default = _search;

/***/ },
/* 42 */
/***/ function(module, exports) {

	module.exports = jQuery;

/***/ },
/* 43 */
/***/ function(module, exports, __webpack_require__) {

	'use strict';

	Object.defineProperty(exports, "__esModule", {
	    value: true
	});

	var _jQuery = __webpack_require__(42);

	var _jQuery2 = _interopRequireDefault(_jQuery);

	function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

	var _mySlider = function _mySlider() {
	    var banner = (0, _jQuery2.default)('.banner'),
	        unslidersss = null;

	    if (banner.length === 0) {
	        return;
	    }

	    // 启动轮播插件
	    unslidersss = banner.unslider({
	        autoplay: true,
	        infinite: true,
	        arrows: false
	    });

	    //  Let's say we want to use $.getScript to load our scripts for some reason
	    var scripts = ['http://stephband.info/jquery.event.move/js/jquery.event.move.js', 'http://stephband.info/jquery.event.swipe/js/jquery.event.swipe.js'];

	    _jQuery2.default.getScript(scripts[0]);

	    //  Once our script is loaded, we can initSwipe to add swipe support
	    _jQuery2.default.getScript(scripts[1], function () {
	        banner.unslider('initSwipe');
	    });
	};

	exports.default = _mySlider;

/***/ },
/* 44 */
/***/ function(module, exports, __webpack_require__) {

	'use strict';

	Object.defineProperty(exports, "__esModule", {
	    value: true
	});

	var _jQuery = __webpack_require__(42);

	var _jQuery2 = _interopRequireDefault(_jQuery);

	var _comb_string = __webpack_require__(45);

	function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

	/**
	 * 发起异步请求，并且根据数据组织好DOM并插入到页面当中
	 * @param  {String}  api  接口
	 * @param {Object}  target  插入的目标对象
	 * @param  {String} dataName  json数据中实际的内容对象名字
	 * @param {Function}  dataToDOMFun  将json中内容组合成DOM节点的函数
	 */
	var getAppendData = function getAppendData(api, target, dataName, dataToDOMFun) {
	    // 注意，后台传过来的json中bool型可能是字符串，而且!"false"的值是true
	    var wuLoad = (0, _jQuery2.default)('.wu_load');

	    wuLoad.attr('disabled', true); // 按钮不可点击
	    wuLoad.val('正在加载...');

	    _jQuery2.default.ajax({
	        url: api,
	        type: 'GET',
	        success: function success(data) {
	            var jsonObj = JSON.parse(data),
	                conData = jsonObj[dataName],
	                tempDOM = '';

	            if (conData.length === 0) {
	                wuLoad.css('display', 'none'); // 隐藏按钮
	                wuLoad.val('点击加载更多');
	                wuLoad.attr('disabled', false); // 按钮可点击
	                (0, _jQuery2.default)('.wu_is-end').css('display', 'block');
	            } else {
	                tempDOM = dataToDOMFun(conData); // 将json中内容组合成DOM节点
	                (0, _jQuery2.default)(target).append(tempDOM); // 在加载更多前面加入新增节点

	                wuLoad.val('点击加载更多');
	                wuLoad.attr('disabled', false); // 按钮可点击
	                (0, _jQuery2.default)('.wu_is-end').css('display', 'none');
	            }
	        },
	        error: function error() {
	            wuLoad.val('点击加载更多');
	            wuLoad.attr('disabled', false); // 按钮可点击
	            (0, _jQuery2.default)('.wu_is-end').css('display', 'none');
	        }
	    });
	};

	/**
	 * 点击加载更多的函数，有多个页面都用到了点击加载更多，需要根据url判断当前页面是哪个页面
	 * @requires  [getAppendData()，在本文件中]
	 * @requires  
	 *     [newsLi()，在'./_comb_string.js']  
	 *     [fashLi()，在'./_comb_string.js']  
	 *     [gameClassContent()，在'./_comb_string.js']
	 *     [videoClassContent()，在'./_comb_string.js']
	 * @return {[type]}
	 */
	var _load_more = function _load_more() {
	    var wuRollLoad = document.getElementsByClassName('roll-load')[0];
	    if (!wuRollLoad) {
	        return;
	    }

	    // document.getElementsByClassName('roll-load')[0].isEnd = 'false';
	    wuRollLoad.isEnd = 'false';

	    // modified by Nicholas 7/26/2016 改成点击加载更多
	    (0, _jQuery2.default)('.wu_load').bind('click', function (e) {
	        e.preventDefault();

	        var rollLoad = document.getElementsByClassName('roll-load')[0],
	            rollLoadIsEnd = rollLoad.isEnd,
	            hadLoadCount = rollLoad.getElementsByTagName('li').length,
	            wantToLoad = 5,
	            host = 'http://' + location.host,
	            // 不知道为什么要加上http://
	        pathType = location.pathname,
	            pathTypeArray = pathType.split('/'),
	            // add by Nicholas 8/5/2016,主要是锐彬修改了项目的目录结构
	        pathTypeArrayLength = pathTypeArray.length,
	            api = '',
	            dataName = 'dataList',
	            // 不同页面返回的内容的对象名字不同 // modified by Nicholas 8/9/2016，现在统一了
	        dataToDOMFun = null; // 不同页面数据组成不同的DOM节点的函数

	        var zixunType = ["newsletter", "observation", "game", "movie"],
	            // add by 锐彬 8/5/2016，主要是修改了项目的目录结构
	        zixunTypeLength = zixunType.length;

	        if (pathTypeArrayLength === 3 && pathType.indexOf("news") > 0) {
	            // 咨询页
	            // 如果location.pathname用‘/’号分开之后的数组长度是3，并且location.pathname中有"news"
	            hadLoadCount = rollLoad.getElementsByTagName('li').length;

	            api = host + '/json/article.do?index=' + hadLoadCount + '&num=' + wantToLoad;

	            dataToDOMFun = _comb_string.fashLi; // 咨询页
	        } else if (pathTypeArrayLength > 3 && pathType.indexOf("news") > 0) {
	            // 资讯页面下的几种分类，以及7*24快讯
	            // 如果location.pathname用‘/’号分开之后的数组长度大于3，并且location.pathname中有"news"
	            if (pathType.indexOf("newsflash") > 0) {
	                // 快讯 7*24
	                hadLoadCount = rollLoad.getElementsByTagName('section').length;

	                api = host + '/json/kuaixun.do?index=' + hadLoadCount + '&num=' + wantToLoad;

	                dataToDOMFun = _comb_string.newsLi; // 快讯 7*24
	            } else {
	                // 资讯下的分类
	                var type = pathTypeArray[pathTypeArrayLength - 2],
	                    typeKey = 0; // zixunType的下标

	                for (var i = 0; i < zixunTypeLength; i++) {
	                    if (type === zixunType[i]) {
	                        typeKey = i + 1;
	                        break;
	                    }
	                }

	                hadLoadCount = rollLoad.getElementsByTagName('li').length;

	                api = host + '/json/type.do?index=' + hadLoadCount + '&num=' + wantToLoad + '&type=' + typeKey;

	                dataToDOMFun = _comb_string.fashLi; // 资讯下的分类
	            }
	        } else if (pathTypeArrayLength === 3 && pathType.indexOf("game") > 0) {
	            // 游戏分类页
	            var navClass = document.getElementsByClassName('nav-class')[0],
	                classify = navClass.classify,
	                filtrate = navClass.filtrate,
	                apiType = '';

	            hadLoadCount = rollLoad.getElementsByTagName('li').length;

	            switch (classify) {
	                case 'game':
	                    // 最新
	                    apiType = '/json/game.do?';
	                    break;
	                case 'gameclass':
	                    // 分类
	                    apiType = '/json/gameclass.do?';
	                    break;
	                case 'gamePaihang':
	                    // 排行
	                    apiType = '/json/gamePaihang.do?';
	                    break;
	                case 'gameTuijie':
	                    // 推荐
	                    apiType = '/json/gameTuijie.do?';
	                    break;
	                default:
	                    apiType = '/json/game.do?';
	            }

	            if (classify === 'gameclass') {
	                api = host + apiType + 'index=' + hadLoadCount + '&num=' + wantToLoad + '&type=' + filtrate.type + '&system=' + filtrate.system + '&device=' + filtrate.device;
	            } else {
	                api = host + apiType + 'index=' + hadLoadCount + '&num=' + wantToLoad;
	            }

	            dataToDOMFun = _comb_string.gameClassContent; // 游戏分类页
	        } else if (pathTypeArrayLength === 3 && pathType.indexOf("video") > 0) {
	            // 视频分类页
	            var _navClass = document.getElementsByClassName('nav-class')[0],
	                _classify = _navClass.classify,
	                _filtrate = _navClass.filtrate,
	                _apiType = '';

	            hadLoadCount = rollLoad.getElementsByTagName('li').length;

	            switch (_classify) {
	                case 'videoZuixin':
	                    // 最新
	                    _apiType = '/json/videoZuixin.do?';
	                    break;
	                case 'videoZuire':
	                    // 最热
	                    _apiType = '/json/videoZuire.do?';
	                    break;
	                case 'video':
	                    // 分类
	                    _apiType = '/json/video.do?';
	                    break;
	                default:
	                    _apiType = '/json/videoZuire.do?';
	            }

	            if (_classify === 'video') {
	                api = host + _apiType + 'index=' + hadLoadCount + '&num=' + wantToLoad + '&type=' + _filtrate.type;
	            } else {
	                api = host + _apiType + 'index=' + hadLoadCount + '&num=' + wantToLoad;
	            }

	            dataToDOMFun = _comb_string.videoClassContent; // 游戏分类页
	        }

	        // 这里仅供测试
	        // api = 'http://10.0.0.16:8080/json/type.do?index=' + hadLoadCount + '&num=' + wantToLoad + '&type=1';
	        // api = 'http://10.0.0.16:8080/json/article.do?index=' + hadLoadCount + '&num=' + wantToLoad;
	        // api = 'http://10.0.0.16:8080/json/kuaixun.do?index=' + hadLoadCount + '&num=' + wantToLoad;
	        // api = 'http://www.haikevr.com/json/kuaixun.do?index=2&num=5';
	        // api = 'http://10.0.0.16:8080' + '/json/gameclass.do?' + 'index=' + 0 + '&num=' + 5 + '&type=' + 0 + '&system=' + 0 + '&device=' + 0;
	        // api = 'http://10.0.0.16:8080' + '/json/game.do?' + 'index=' + 0 + '&num=' + 5;
	        // api = 'http://10.0.0.16:8080' + '/json/gamePaihang.do?' + 'index=' + 0 + '&num=' + 5;
	        // api = 'http://10.0.0.16:8080' + '/json/gameTuijie.do?' + 'index=' + 0 + '&num=' + 5;
	        // api = 'http://10.0.0.16:8080//json/video.do?index=0&num=1&type=1';
	        // api = 'http://10.0.0.16:8080//json/videoZuixin.do?index=0&num=1';
	        // api = 'http://10.0.0.16:8080/json/videoZuire.do?index=0&num=1';
	        // dataToDOMFun = videoClassContent; // 游戏分类页

	        getAppendData(api, rollLoad, dataName, dataToDOMFun); // 发起异步请求，并且根据数据组织好DOM并插入到页面当中
	    });
	};

	exports.default = _load_more;

/***/ },
/* 45 */
/***/ function(module, exports, __webpack_require__) {

	'use strict';

	Object.defineProperty(exports, "__esModule", {
	    value: true
	});
	exports.videoClassContent = exports.gameClassContent = exports.fashLi = exports.newsLi = undefined;

	var _public_fun = __webpack_require__(46);

	// 7*24 快讯页面，根据数据组织要展示的内容
	var newsLi = function newsLi(news) {
	    var tempDOM = '',
	        newsCount = news.length,
	        dayNews = null,
	        dayNewsCount = 0;

	    for (var i = 0; i < newsCount; i++) {
	        dayNews = news[i]; // 每天的新闻
	        dayNewsCount = dayNews.length;

	        tempDOM += '<section class="w_flash-day font-size-2 font-color-4 clear-f">';
	        tempDOM += '<ol class="fl w_time">';
	        tempDOM += '<li>' + (0, _public_fun.format)(dayNews[0].createTime, '年月日', 'cn') + '<em class="w_dot"></em></li>';
	        for (var j = 0; j < dayNewsCount; j++) {
	            tempDOM += '<li>' + (0, _public_fun.format)(dayNews[j].createTime, '月日 时:分', 'cn') + '<em class="w_dot"></em></li>';
	        }
	        tempDOM += '</ol>';

	        tempDOM += '<ul class="w_flash-detail fr">';
	        for (var k = 0; k < dayNewsCount; k++) {
	            tempDOM += '<li>';
	            tempDOM += '<h2 class="w_h2">';
	            tempDOM += '<a rel="nofollow" class="font-size-1 single-ellipsis">' + dayNews[k].title + '</a>';
	            tempDOM += '</h2>';
	            tempDOM += '<div class="multi-ellipsis">';
	            tempDOM += '<p class="font-size-4">' + dayNews[k].preinfo + '</p>';
	            tempDOM += '</div>';
	            tempDOM += '<a class="w_flash-btn font-size-4" href="' + dayNews[k].link + '" rel="nofollow">阅读原文</a>';
	            tempDOM += '</li>';
	        }
	        tempDOM += '</ul>';
	        tempDOM += '</section>';
	    }

	    return tempDOM;
	};

	// 资讯页面，根据数据组织要展示的内容
	/*
	    各个页面异步请求的数据，将其组成字符串，供页面展示
	*/

	var fashLi = function fashLi(articles) {
	    var tempDOM = '',
	        articleCount = articles.length,
	        zixunType = ["newsletter", "observation", "game", "movie"];

	    for (var i = 0; i < articleCount; i++) {
	        tempDOM += '<li class="listbox">';
	        tempDOM += '<a class="_image" href="news/' + zixunType[articles[i].type - 1] + '/' + articles[i].articleId + '.html">';
	        tempDOM += '<img src="' + articles[i].preimage + '" alt="' + articles[i].title + '" />';
	        tempDOM += '<span class="font-color-5">头条</span>';
	        tempDOM += '</a>';
	        tempDOM += '<div class="_text flex-1">';
	        tempDOM += '<a class="multi-ellipsis font-color-3" href="news/' + zixunType[articles[i].type - 1] + '/' + articles[i].articleId + '.html">';
	        tempDOM += '<h2>' + articles[i].title + '</h2>';
	        tempDOM += '</a>';
	        tempDOM += '<div>';
	        tempDOM += '<a class="tag" href="news/' + zixunType[articles[i].type - 1] + '/">';

	        switch (articles[i].type) {
	            case 1:
	                tempDOM += '时讯';
	                break;
	            case 2:
	                tempDOM += '观察';
	                break;
	            case 3:
	                tempDOM += '游戏';
	                break;
	            case 4:
	                tempDOM += '影视';
	                break;
	            default:
	                tempDOM += '';
	        }
	        tempDOM += '</a>';
	        tempDOM += '<span class="font-size-2">' + (0, _public_fun.format)(articles[i].createTime, 'yyyy年MM月dd日', 'en') + '</span>';
	        tempDOM += '</div>';
	        tempDOM += '</div>';
	        tempDOM += '</li>';
	    }

	    return tempDOM;
	};

	// 游戏分类页，根据数据组织要展示的内容
	var gameClassContent = function gameClassContent(data) {
	    var dom = '',
	        tempData = null,
	        dataCount = data.length,
	        youxiMap = {
	        "漫游体验": "roaming",
	        "角色扮演": "role",
	        "冒险解谜": "adventure",
	        "第一人称射击": "shooting",
	        "休闲益智": "puzzle",
	        "飞行射击": "flight",
	        "即时战略": "strategy",
	        "恐怖游戏": "horror",
	        "动作游戏": "action",
	        "格斗游戏": "fighting",
	        "赛车竞速": "racing",
	        "其他": "typeothers"
	    };

	    for (var i = 0; i < dataCount; i++) {
	        tempData = data[i];

	        dom += '<li>';
	        dom += '<a href="' + 'game/' + youxiMap[tempData.type] + '/' + tempData.gameId + '.html' + '" class="game-intro">';
	        dom += '<img src="' + tempData.preimage + '" alt="' + tempData.title + '" />';
	        dom += '<div class="font-color-5 game-detail">';
	        dom += '<h2 class="font-size-9">' + tempData.title + '</h2>';
	        dom += '<p class="font-color-4 font-size-11">' + tempData.preinfo + '</p>';
	        dom += '<div class="iconfont center-between">';
	        dom += '<span class="font-size-8">' + (0, _public_fun.format)(tempData.createTime, 'yyyy-MM-dd HH:mm', 'en') + '</span>';
	        dom += '<div class="center-center">';
	        dom += '<em class="font-size-7">&#xe60d;</em>';
	        dom += '<span class="font-size-9">' + tempData.downloadNum + '</span>';
	        dom += '</div>';
	        dom += '</div>';
	        dom += '</div>';
	        dom += '</a>';
	        dom += '</li>';
	    }

	    return dom;
	};

	// 视频分类页，根据数据组织要展示的内容
	var videoClassContent = function videoClassContent(data) {
	    var dom = '',
	        tempData = null,
	        dataCount = data.length,
	        shipinType = ["experience", "entertainment", "entertainment", "entertainment", "entertainment", "entertainment", "life", "life", "life", "life", "life", "technology", "technology", "technology", "fashion", "scenery", "scenery", "scenery", "society", "society", "entertainment", "entertainment", "entertainment", "entertainment", "entertainment", "entertainment", "entertainment", "society", "technology", "technology", "entertainment", "entertainment", "life", "technology", "scenery", "fashion", "experience", "society", "life", "life", "experience", "experience", "experience"];

	    for (var i = 0; i < dataCount; i++) {
	        tempData = data[i];

	        dom += '<li>';
	        dom += '<a href="' + 'video/' + shipinType[tempData.type] + '/' + tempData.videoId + '.html' + '" alt="' + tempData.title + '" class="index-video">';
	        dom += '<img src="' + tempData.preimage + '" alt="' + tempData.title + '" />';
	        dom += '<div class="video-detail center-between">';
	        dom += '<div class="font-color-5">';
	        dom += '<p class="font-size-9 single-ellipsis">' + tempData.title + '</p>';
	        dom += '<p class="font-size-10">' + tempData.videoLength + '</p>';
	        dom += '</div>';
	        dom += '<span class="iconfont font-color-5">&#xe610;</span>';
	        dom += '</div>';
	        dom += '<div class="bg-black-gradual"></div>';
	        dom += '</a>';
	        dom += '</li>';
	    }

	    return dom;
	};

	exports.newsLi = newsLi;
	exports.fashLi = fashLi;
	exports.gameClassContent = gameClassContent;
		exports.videoClassContent = videoClassContent;

/***/ },
/* 46 */
/***/ function(module, exports) {

	'use strict';

	Object.defineProperty(exports, "__esModule", {
	    value: true
	});
	/**
	 * 格式化时间
	 * @param  {Number}  time  时间，以1970年开始，换算成秒
	 * @param  {String}  format  时间的格式
	 * @param  {String}  lang  格式的语言
	 * @example 
	 *      format(1470240000000, 'yyyy年MM月dd日', 'cn')
	 *      format(1470240000000, 'yyyy-MM-dd HH:mm', 'en')
	 * @return {String}  格式化的时间
	 */
	var format = function format(time, _format, lang) {
	    var t = new Date(time);
	    var tf = function tf(i) {
	        return (i < 10 ? '0' : '') + i;
	    };

	    if (lang === 'en') {
	        return _format.replace(/yyyy|MM|dd|HH|mm|ss/g, function (a) {
	            switch (a) {
	                case 'yyyy':
	                    return tf(t.getFullYear());
	                    break;
	                case 'MM':
	                    return tf(t.getMonth() + 1);
	                    break;
	                case 'mm':
	                    return tf(t.getMinutes());
	                    break;
	                case 'dd':
	                    return tf(t.getDate());
	                    break;
	                case 'HH':
	                    return tf(t.getHours());
	                    break;
	                case 'ss':
	                    return tf(t.getSeconds());
	                    break;
	            }
	        });
	    } else {
	        return _format.replace(/年|月|日|时|分|秒/g, function (a) {
	            switch (a) {
	                case '年':
	                    return tf(t.getFullYear()) + '年';
	                    break;
	                case '月':
	                    return tf(t.getMonth() + 1) + '月';
	                    break;
	                case '分':
	                    return tf(t.getMinutes());
	                    break;
	                case '日':
	                    return tf(t.getDate()) + '日';
	                    break;
	                case '时':
	                    return tf(t.getHours());
	                    break;
	                case '秒':
	                    return tf(t.getSeconds());
	                    break;
	            }
	        });
	    }
	};

	exports.format = format;

/***/ },
/* 47 */
/***/ function(module, exports, __webpack_require__) {

	'use strict';

	Object.defineProperty(exports, "__esModule", {
	    value: true
	});

	var _jQuery = __webpack_require__(42);

	var _jQuery2 = _interopRequireDefault(_jQuery);

	function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

	// 缓慢滚动到目标
	var goToByScroll = function goToByScroll(id) {
	    (0, _jQuery2.default)('html,body').animate({
	        scrollTop: (0, _jQuery2.default)('#' + id).offset().top
	    }, 'slow');
	};

	// 参数target：是需要绑定点击事件的按钮的class
	var _scroll_to = function _scroll_to(target) {
	    var tar = (0, _jQuery2.default)('.' + target),
	        tarCount = tar.length;

	    if (tarCount === 0) {
	        return false;
	    }

	    tar.bind('click', function (e) {
	        e.preventDefault();

	        var name = (0, _jQuery2.default)(this).attr('name');

	        if (!name) {
	            return false;
	        }

	        goToByScroll(name);
	    });
	};

	exports.default = _scroll_to;

/***/ },
/* 48 */
/***/ function(module, exports, __webpack_require__) {

	'use strict';

	Object.defineProperty(exports, "__esModule", {
	    value: true
	});

	var _jQuery = __webpack_require__(42);

	var _jQuery2 = _interopRequireDefault(_jQuery);

	var _comb_string = __webpack_require__(45);

	function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

	/**
	 * 异步获取数据，并且将数据组合成DOM之后插入目标节点
	 * @param  {String} api  接口
	 * @param  {String} type  请求的类型
	 * @param  {Object} target  插入的目标DOM
	 * @param  {String} dataName  json数据中实际的内容对象名字
	 * @param  {Function} myCallBack  将json中内容组合成DOM节点的函数
	 */
	/**
	 * 这个js文件主要是给视频分类页以及游戏分类页的分类导航栏用
	 */

	var getAppendData = function getAppendData(api, type, target, dataName, myCallBack) {
	    _jQuery2.default.ajax({
	        url: api,
	        type: type,
	        success: function success(data) {
	            var jsonObj = JSON.parse(data),
	                content = jsonObj[dataName],
	                tempDOM = '';

	            if (content.length === 0) {
	                (0, _jQuery2.default)('.wu_load').css('display', 'none'); // 隐藏点击加载更多按钮
	                (0, _jQuery2.default)('.wu_is-end').css('display', 'block'); // 显示已经没有数据的提示
	            } else {
	                tempDOM = myCallBack(content);
	                (0, _jQuery2.default)('.wu_load').css('display', 'block'); // 显示点击加载更多按钮
	                (0, _jQuery2.default)('.wu_is-end').css('display', 'none'); // 隐藏已经没有数据的提示
	            }

	            target.html(tempDOM);
	        }
	    });
	};

	/**
	 * 游戏以及视频分类页中“分类”按钮的交互效果，淡入淡出
	 * @param {Object}  target  这个一个jQuery对象，也是分类中要弹出的对象DOM
	 * @param {Boolean}  isGame  表示是不是游戏分类页，如果是游戏分类页的话需要重置弹框的高度
	 * @return {Boolean} 没有目标对象的话，返回false
	 */
	var classifyInteraction = function classifyInteraction(target, isGame) {
	    var display = '',
	        shadeHeight = document.body.scrollHeight; // 网页正文全文高

	    if (target.length === 0) {
	        return false;
	    }

	    if (isGame) {
	        // 游戏分类页的话需要重置弹框的高度
	        target.css('height', shadeHeight); // 重置弹窗的高度为整个网页文档的高度
	    }

	    display = target.css('display');

	    if (display === 'none') {
	        target.stop().fadeIn('slow');
	    } else {
	        target.stop().fadeOut('slow');
	    }
	};

	/**
	 * 游戏分类页中“分类”里面的各个选项的交互效果
	 * @param  {Object}  self  jQuery的对象
	 * @param  {Boolean}  isAll  标志是不是“全部”，这种按钮
	 */
	var gameClassBtnChange = function gameClassBtnChange(self, isAll) {
	    var gameClassAllBtn = (0, _jQuery2.default)('.game-class-all'),
	        gameClassBtn = (0, _jQuery2.default)('.game-class-btn'),
	        originAllBtn = '',
	        originClassBtn = '',
	        allBtnHasCurClass = '',
	        allBtnNotHasCurClass = '',
	        classBtnHasCurClass = '',
	        classBtnNotHasCurClass = '';

	    originAllBtn = gameClassAllBtn.attr('class');
	    originClassBtn = gameClassBtn.attr('class');

	    allBtnNotHasCurClass = originAllBtn.replace('game-class-cur-btn', '');
	    allBtnHasCurClass = allBtnNotHasCurClass + ' game-class-cur-btn ';

	    classBtnNotHasCurClass = originClassBtn.replace('game-class-cur-btn', '');
	    classBtnHasCurClass = classBtnNotHasCurClass + ' game-class-cur-btn ';

	    if (isAll) {
	        // 当用户点击“全部”时的交互
	        self.attr('class', allBtnHasCurClass);
	        self.siblings().children('.game-class-btn').attr('class', classBtnNotHasCurClass);
	    } else {
	        // 当用户点击除了“全部”之外的其它按钮时的交互
	        self.parent().children('.game-class-btn').attr('class', classBtnNotHasCurClass);
	        self.parent().parent().children('.game-class-all').attr('class', allBtnNotHasCurClass);
	        self.attr('class', classBtnHasCurClass);
	    }
	};

	/**
	 * 游戏分类页中“分类”里面的各个选项的逻辑处理部分
	 * @param  {Object}  self  jQuery的对象
	 * @param  {String}  host  网站的域名
	 * @param  {Boolean}  isAll  标志是不是“全部”，这种按钮
	 * @requires  [getAppendData()，在本文件内]  [gameClassContent()，在'./_comb_string.js'中]
	 */
	var gameClassBtnLogic = function gameClassBtnLogic(self, host) {
	    var name = self.attr('name'),
	        // 分类的类型以及选项的flag都放在name属性中，用-分开
	    nameArr = [],
	        classTypeName = '',
	        // 用来标示是哪个类型，例如“游戏类型”、“运行平台”、“显示设备”
	    classTypeFlag = '',
	        // 用来标示类型下的第几个选项
	    navClass = document.getElementsByClassName('nav-class')[0],
	        filtrate = {},
	        api = '',
	        type = 'GET',
	        target = (0, _jQuery2.default)('.game-class-container');

	    if (!name) {
	        return false;
	    }

	    nameArr = name.split('-');
	    classTypeName = nameArr[0];
	    classTypeFlag = nameArr[1];

	    navClass.filtrate[classTypeName] = classTypeFlag;

	    filtrate = navClass.filtrate;

	    api = host + '/json/gameclass.do?index=0&num=6&type=' + filtrate.type + '&system=' + filtrate.system + '&device=' + filtrate.device;

	    // 异步获取数据，并且将数据组合成DOM之后插入目标节点
	    getAppendData(api, type, target, 'dataList', _comb_string.gameClassContent);
	};

	/**
	 * 游戏分类页面的主函数
	 * @param  {String}  host  网站的域名
	 * @requires [jQuery]  [getAppendData()，在本文件内]  [gameClassBtnChange()，在本文件内]  [gameClassBtnLogic()，在本文件内]
	 * @return {Boolean}  当没有相应的目标，即不是在游戏页面时返回false
	 */
	var gameClass = function gameClass(host) {
	    // “最新”、“推荐”、“排行”、“分类”等按钮的点击事件
	    (0, _jQuery2.default)('.classify-btn').bind('click', function (e) {
	        e.preventDefault(); // 组织默认事件

	        var self = (0, _jQuery2.default)(this),
	            btnName = self.attr('name'),
	            // btn的名字，用于标示是那个btn
	        navClassLi = (0, _jQuery2.default)('.nav-class-li'),
	            originClass = navClassLi.attr('class'),
	            hasCurClass = '',
	            notHasCurClass = '',
	            api = '',
	            type = 'GET',
	            target = (0, _jQuery2.default)('.game-class-container'),
	            navClass = document.getElementsByClassName('nav-class')[0];

	        notHasCurClass = originClass.replace('nav-li-cur', ''), hasCurClass = notHasCurClass + ' nav-li-cur ';

	        navClassLi.attr('class', notHasCurClass); // 去掉当前导航栏的样式
	        self.parent('.nav-class-li').attr('class', hasCurClass); // 给当前导航栏添加样式

	        switch (btnName) {
	            case 'new':
	                // 最新
	                navClass.classify = 'game'; // 设置页面的标志，主要是给点击加载更多用的
	                api = host + '/json/game.do?index=0&num=6'; // 这里写死了从第0条开始，取6条数据
	                (0, _jQuery2.default)('.game-class ul').stop().fadeOut('slow'); // 隐藏分类按钮的弹窗
	                break;
	            case 'recommend':
	                // 推荐
	                navClass.classify = 'gameTuijie'; // 设置页面的标志，主要是给点击加载更多用的
	                api = host + '/json/gameTuijie.do?index=0&num=6'; // 这里写死了从第0条开始，取6条数据
	                (0, _jQuery2.default)('.game-class ul').stop().fadeOut('slow'); // 隐藏分类按钮的弹窗
	                break;
	            case 'rankList':
	                // 排行
	                navClass.classify = 'gamePaihang'; // 设置页面的标志，主要是给点击加载更多用的
	                api = host + '/json/gamePaihang.do?index=0&num=6'; // 这里写死了从第0条开始，取6条数据
	                (0, _jQuery2.default)('.game-class ul').stop().fadeOut('slow'); // 隐藏分类按钮的弹窗
	                break;
	            case 'classify':
	                // 分类
	                navClass.classify = 'gameclass'; // 设置页面的标志，主要是给点击加载更多用的
	                classifyInteraction((0, _jQuery2.default)('.game-class ul'), true); // 游戏分类页中“分类”按钮的交互效果，淡入淡出
	                break;
	            default:
	                return false;
	        }

	        if (!api || target.length === 0) {
	            // api是空，不再需要执行下面的操作
	            return false;
	        }

	        // 异步获取数据，并且将数据组合成DOM之后插入目标节点
	        getAppendData(api, type, target, 'dataList', _comb_string.gameClassContent);
	    });

	    // 给游戏分类的弹窗绑定点击退出事件
	    (0, _jQuery2.default)('.game-class ul').bind('click', function () {
	        (0, _jQuery2.default)(this).stop().fadeOut('slow');
	    });

	    // 给游戏分类的弹窗中的每一个选项绑定点击事件
	    (0, _jQuery2.default)('.game-class-btn').bind('click', function (e) {
	        e.preventDefault(); // 组织默认事件
	        e.stopPropagation(); // 组织时间冒泡

	        var self = (0, _jQuery2.default)(this);

	        gameClassBtnChange(self, false); // 点击按钮后的交互效果
	        gameClassBtnLogic(self, host); // 点击按钮后的处理逻辑，注意，这里的host用了父级作用域的host
	    });

	    // 给游戏分类的弹窗中的“全部”按钮添加点击事件
	    (0, _jQuery2.default)('.game-class-all').bind('click', function (e) {
	        e.preventDefault(); // 组织默认事件
	        e.stopPropagation(); // 组织时间冒泡

	        var self = (0, _jQuery2.default)(this);

	        gameClassBtnChange(self, true); // 点击按钮后的交互效果
	        gameClassBtnLogic(self, host); // 点击按钮后的处理逻辑，注意，这里的host用了父级作用域的host
	    });
	};

	/**
	 * 视频分类页中“分类”里面的各个选项的逻辑处理部分
	 * @param  {Object}  self  jQuery的对象
	 * @param  {String}  host  网站的域名
	 * @param  {Boolean}  isAll  标志是不是“全部”，这种按钮
	 * @requires  [getAppendData()，在本文件内]  [videoClassContent()，在'./_comb_string.js'中]
	 */
	var videoClassBtnLogic = function videoClassBtnLogic(self, host) {
	    var name = self.attr('name'),
	        // 分类的类型放在name属性中
	    navClass = document.getElementsByClassName('nav-class')[0],
	        api = '',
	        type = 'GET',
	        target = (0, _jQuery2.default)('.video-class-container');

	    if (!name) {
	        return false;
	    }

	    navClass.filtrate['type'] = name;

	    api = host + '/json/video.do?index=0&num=6&type=' + name;

	    // 异步获取数据，并且将数据组合成DOM之后插入目标节点
	    getAppendData(api, type, target, 'dataList', _comb_string.videoClassContent);
	};

	/**
	 * 视频分类页面的主函数
	 * @param  {String}  host  网站的域名
	 * @requires [jQuery]  [getAppendData()，在本文件内]  [gameClassBtnChange()，在本文件内]  [gameClassBtnLogic()，在本文件内]
	 * @return {Boolean}  当没有相应的目标，即不是在游戏页面时返回false
	 */
	var videoClass = function videoClass(host) {
	    // “最新”、“推荐”、“排行”、“分类”等按钮的点击事件
	    (0, _jQuery2.default)('.classify-btn').bind('click', function (e) {
	        e.preventDefault(); // 组织默认事件

	        var self = (0, _jQuery2.default)(this),
	            btnName = self.attr('name'),
	            // btn的名字，用于标示是那个btn
	        navClassLi = (0, _jQuery2.default)('.nav-class-li'),
	            originClass = navClassLi.attr('class'),
	            hasCurClass = '',
	            notHasCurClass = '',
	            api = '',
	            type = 'GET',
	            target = (0, _jQuery2.default)('.video-class-container'),
	            navClass = document.getElementsByClassName('nav-class')[0];

	        notHasCurClass = originClass.replace('nav-li-cur', ''), hasCurClass = notHasCurClass + ' nav-li-cur ';

	        navClassLi.attr('class', notHasCurClass); // 去掉当前导航栏的样式
	        self.parent('.nav-class-li').attr('class', hasCurClass); // 给当前导航栏添加样式

	        switch (btnName) {
	            case 'new':
	                // 最新
	                navClass.classify = 'videoZuixin'; // 设置页面的标志，主要是给点击加载更多用的
	                api = host + '/json/videoZuixin.do?index=0&num=6'; // 这里写死了从第0条开始，取6条数据
	                (0, _jQuery2.default)('.video-class ul').stop().fadeOut('slow'); // 隐藏分类按钮的弹窗
	                break;
	            case 'hot':
	                // 最热
	                navClass.classify = 'videoZuire'; // 设置页面的标志，主要是给点击加载更多用的
	                api = host + '/json/videoZuire.do?index=0&num=6'; // 这里写死了从第0条开始，取6条数据
	                (0, _jQuery2.default)('.video-class ul').stop().fadeOut('slow'); // 隐藏分类按钮的弹窗
	                break;
	            case 'classify':
	                // 分类
	                navClass.classify = 'video'; // 设置页面的标志，主要是给点击加载更多用的
	                classifyInteraction((0, _jQuery2.default)('.video-class ul'), false); // 游戏分类页中“分类”按钮的交互效果，淡入淡出
	                break;
	            default:
	                return false;
	        }

	        if (!api || target.length === 0) {
	            // api是空，不再需要执行下面的操作
	            return false;
	        }

	        // 异步获取数据，并且将数据组合成DOM之后插入目标节点
	        getAppendData(api, type, target, 'dataList', _comb_string.videoClassContent);
	    });

	    // 给视频分类的弹窗中的每一个选项绑定点击事件
	    (0, _jQuery2.default)('.vedio-class-btn').bind('click', function (e) {
	        e.preventDefault(); // 组织默认事件
	        e.stopPropagation(); // 组织时间冒泡

	        var self = (0, _jQuery2.default)(this);

	        (0, _jQuery2.default)('.video-class ul').stop().fadeOut('slow'); // 隐藏分类按钮的弹窗

	        videoClassBtnLogic(self, host); // 点击按钮后的处理逻辑，注意，这里的host用了父级作用域的host
	    });
	};

	var _classify = function _classify() {
	    var host = 'http://' + location.host,
	        // 不知道为什么要加上http://
	    // host = 'http://' + '10.0.0.16:8080', // 不知道为什么要加上http:// 测试
	    pathType = location.pathname,
	        pathTypeArray = pathType.split('/'),
	        pathTypeArrayLength = pathTypeArray.length,
	        navClass = document.getElementsByClassName('nav-class')[0];

	    if (!navClass) {
	        return false;
	    }

	    navClass.classify = 'game';

	    navClass.filtrate = {
	        type: 0,
	        system: 0,
	        device: 0
	    };

	    if (pathTypeArrayLength === 3 && pathType.indexOf("game") > 0) {
	        // 游戏分类页
	        gameClass(host);
	    }
	    if (pathTypeArrayLength === 3 && pathType.indexOf("video") > 0) {
	        // 视频分类页
	        videoClass(host);
	    }
	};

	exports.default = _classify;

/***/ }
/******/ ]);
//# sourceMappingURL=bundle.js.map